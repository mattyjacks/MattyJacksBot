# Moltbook + OpenClaw v1 Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# LOCAL PATHS (Windows PC)
# =============================================================================
# Default sync root (will be created if it doesn't exist)
SYNC_ROOT=%USERPROFILE%\Documents\Moltbook\v1

# =============================================================================
# VAST.AI SSH CONNECTION
# =============================================================================
VAST_HOST=
VAST_PORT=22
VAST_USER=root
VAST_SSH_KEY_PATH=~/.ssh/id_rsa
# Or use password (not recommended):
# VAST_PASSWORD=

# =============================================================================
# OLLAMA / MODEL CONFIGURATION
# =============================================================================
# Force Ollama as backend (do not change for v1)
MODEL_BACKEND=ollama

# Default model family (auto-selects quant based on VRAM)
MODEL_FAMILY=qwen3-coder

# Override specific model (optional, bypasses auto-select)
# MODEL_OVERRIDE=qwen3-coder:14b-q4_K_M

# VRAM thresholds for quant selection (in GB)
VRAM_THRESHOLD_LOW=10
VRAM_THRESHOLD_MID=12
VRAM_THRESHOLD_HIGH=16

# =============================================================================
# OPENCLAW CONFIGURATION
# =============================================================================
# OpenClaw gateway port on the Vast instance
OPENCLAW_GATEWAY_PORT=18789

# Workspace path on remote (relative to home)
OPENCLAW_WORKSPACE=~/moltbook/v1/agent_runtime/workspace

# =============================================================================
# MOLTBOOK SKILL
# =============================================================================
# Moltbook posting mode: readonly | approval | autonomous
MOLTBOOK_MODE=readonly

# Moltbook heartbeat interval (hours)
MOLTBOOK_HEARTBEAT_HOURS=4

# =============================================================================
# TELEGRAM BOT (optional control interface)
# =============================================================================
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USER_IDS=

# =============================================================================
# WEB UI
# =============================================================================
# Local UI server port
UI_PORT=3333

# Auth token for the web UI (generated on first run if empty)
UI_AUTH_TOKEN=

# Enable public hosting on Vast (requires additional setup)
UI_PUBLIC_HOST=false

# =============================================================================
# SYNC CONFIGURATION
# =============================================================================
# Conflict policy: newest | pc_wins | vast_wins | keep_both
SYNC_CONFLICT_POLICY=newest

# Backup directory for overwritten files
SYNC_BACKUP_DIR=.sync_backups

# Sync interval in seconds (0 = manual only)
SYNC_INTERVAL=0

# =============================================================================
# SECURITY
# =============================================================================
# Sandbox non-main sessions (recommended: true)
SANDBOX_NON_MAIN=true

# Require approval for Moltbook posts
REQUIRE_POST_APPROVAL=true

# Skill allowlist (comma-separated, empty = allow all bundled)
SKILL_ALLOWLIST=moltbook
